# Pinecone — create index with dimension=1536, metric=cosine
PINECONE_API_KEY=pcsk_xxx
PINECONE_INDEX_NAME=university-rag

# Anthropic — Claude Opus 4.6 for agent, Haiku for vision/ingestion
ANTHROPIC_API_KEY=sk-ant-xxx

# Cohere — embed-v4.0 + rerank-v3.5
COHERE_API_KEY=xxx

# GCP Project (Firestore + Google Drive)
GOOGLE_CLOUD_PROJECT=your-project-id

# Admin API Key (protect admin endpoints, generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
ADMIN_API_KEY=xxx
